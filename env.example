# OpenStreetMap API - Completely FREE, no API key needed!
# The app now uses OSM data instead of Google Places API

# Google Gemini API Key (only needed for AI enquiry generation)
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here
